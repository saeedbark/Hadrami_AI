import 'package:flutter/material.dart';

class AppRadius {
  static BorderRadius sm = BorderRadius.circular(6);
  static BorderRadius md = BorderRadius.circular(10);
  static BorderRadius lg = BorderRadius.circular(14);
  static BorderRadius xl = BorderRadius.circular(20);
  static BorderRadius full = BorderRadius.circular(100);
}
