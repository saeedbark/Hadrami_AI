import 'package:flutter/material.dart';

class AppRadius {
  static BorderRadius sm = BorderRadius.circular(8);
  static BorderRadius md = BorderRadius.circular(12);
  static BorderRadius lg = BorderRadius.circular(16);
  static BorderRadius xl = BorderRadius.circular(22);
  static BorderRadius full = BorderRadius.circular(100);
}
